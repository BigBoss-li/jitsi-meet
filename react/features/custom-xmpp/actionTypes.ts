/**
 * The type of (redux) action which signals that a custom XMPP command should
 * be sent to a specific participant.
 */
export const SEND_CUSTOM_XMPP_COMMAND = 'SEND_CUSTOM_XMPP_COMMAND';

/**
 * The type of (redux) action which signals that a custom XMPP message has been
 * received from another participant.
 */
export const CUSTOM_XMPP_EVENT_RECEIVED = 'CUSTOM_XMPP_EVENT_RECEIVED';
